# Default values for helm-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: avinashdere99/onlinebookstore:tag
  pullPolicy: Always

service:
  type: LoadBalncer
  port: 80


